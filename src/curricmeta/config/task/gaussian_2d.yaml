# 2D Gaussian classification task with easy->hard curriculum
name: gaussian_2d

n_classes: 3
dim: 2
n_samples_per_stage: 2000
batch_size: 128

# Optional override of stds per curriculum stage (easy -> hard)
stage_stds: [0.05, 0.15, 0.3, 0.5]
mean_scale: 2.0

# Optional task-local seed (falls back to global seed if omitted)
# seed: 1234
